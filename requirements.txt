# Core dependencies (required for selection module)
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
pyyaml>=6.0
matplotlib>=3.7
tqdm>=4.65

# Optional: faster typicality computation
# faiss-cpu>=1.7

# Optional: SSL feature extraction (requires GPU + CSAL-3D repo)
# torch>=2.0
# nibabel>=5.0

# Optional: embedding visualization
# umap-learn>=0.5

# Development / testing
# pytest>=7.4
# pytest-cov>=4.0
